# XenozMCP Environment Variables
# Copy to .env and fill in your values

# Vault encryption key (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
XENOZ_VAULT_KEY=

# Or set your Open Cloud API key here (will be auto-stored in vault)
# ROBLOX_OPEN_CLOUD_API_KEY=
