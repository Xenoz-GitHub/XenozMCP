{
  "name": "xenoz-mcp",
  "version": "0.1.0",
  "description": "XenozMCP — AI neural bridge for Roblox Studio. Encrypted MCP server (AES-256-GCM) connecting AI minds to the Roblox metaverse through stdio + WebSocket.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "xenoz-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.8.0",
    "axios": "^1.18.1",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.8.0"
  }
}
